import { DeliveryConditions } from './delivery-conditions';

describe('DeliveryConditions', () => {
  it('should create an instance', () => {
    expect(new DeliveryConditions()).toBeTruthy();
  });
});
