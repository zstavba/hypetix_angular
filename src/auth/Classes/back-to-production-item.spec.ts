import { BackToProductionItem } from './back-to-production-item';

describe('BackToProductionItem', () => {
  it('should create an instance', () => {
    expect(new BackToProductionItem()).toBeTruthy();
  });
});
