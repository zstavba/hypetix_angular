import { AlternativeChippersPartners } from './alternative-chippers-partners';

describe('AlternativeChippersPartners', () => {
  it('should create an instance', () => {
    expect(new AlternativeChippersPartners()).toBeTruthy();
  });
});
