import { TechnologicalProcesses } from './technological-processes';

describe('TechnologicalProcesses', () => {
  it('should create an instance', () => {
    expect(new TechnologicalProcesses()).toBeTruthy();
  });
});
