import { WarehouseFabricSaved } from './warehouse-fabric-saved';

describe('WarehouseFabricSaved', () => {
  it('should create an instance', () => {
    expect(new WarehouseFabricSaved()).toBeTruthy();
  });
});
