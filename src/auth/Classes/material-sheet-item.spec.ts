import { MaterialSheetItem } from './material-sheet-item';

describe('MaterialSheetItem', () => {
  it('should create an instance', () => {
    expect(new MaterialSheetItem()).toBeTruthy();
  });
});
