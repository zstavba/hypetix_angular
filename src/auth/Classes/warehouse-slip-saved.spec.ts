import { WarehouseSlipSaved } from './warehouse-slip-saved';

describe('WarehouseSlipSaved', () => {
  it('should create an instance', () => {
    expect(new WarehouseSlipSaved()).toBeTruthy();
  });
});
