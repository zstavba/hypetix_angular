import { ClassificationWorkProceduress } from './classification-work-proceduress';

describe('ClassificationWorkProceduress', () => {
  it('should create an instance', () => {
    expect(new ClassificationWorkProceduress()).toBeTruthy();
  });
});
