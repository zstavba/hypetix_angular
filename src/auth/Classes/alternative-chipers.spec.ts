import { AlternativeChipers } from './alternative-chipers';

describe('AlternativeChipers', () => {
  it('should create an instance', () => {
    expect(new AlternativeChipers()).toBeTruthy();
  });
});
