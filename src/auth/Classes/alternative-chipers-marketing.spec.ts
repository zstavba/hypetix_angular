import { AlternativeChipersMarketing } from './alternative-chipers-marketing';

describe('AlternativeChipersMarketing', () => {
  it('should create an instance', () => {
    expect(new AlternativeChipersMarketing()).toBeTruthy();
  });
});
