import { SearchSectorPipe } from './search-sector.pipe';

describe('SearchSectorPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSectorPipe();
    expect(pipe).toBeTruthy();
  });
});
