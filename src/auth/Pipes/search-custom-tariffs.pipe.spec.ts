import { SearchCustomTariffsPipe } from './search-custom-tariffs.pipe';

describe('SearchCustomTariffsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCustomTariffsPipe();
    expect(pipe).toBeTruthy();
  });
});
