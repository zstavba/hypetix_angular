import { SearchZipCodePipe } from './search-zip-code.pipe';

describe('SearchZipCodePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchZipCodePipe();
    expect(pipe).toBeTruthy();
  });
});
