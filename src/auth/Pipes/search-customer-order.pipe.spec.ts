import { SearchCustomerOrderPipe } from './search-customer-order.pipe';

describe('SearchCustomerOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCustomerOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
