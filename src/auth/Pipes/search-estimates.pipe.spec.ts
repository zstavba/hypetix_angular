import { SearchEstimatesPipe } from './search-estimates.pipe';

describe('SearchEstimatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEstimatesPipe();
    expect(pipe).toBeTruthy();
  });
});
