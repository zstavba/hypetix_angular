import { SearchCreditsPipe } from './search-credits.pipe';

describe('SearchCreditsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCreditsPipe();
    expect(pipe).toBeTruthy();
  });
});
