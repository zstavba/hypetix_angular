import { SearchCurrenciePipe } from './search-currencie.pipe';

describe('SearchCurrenciePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCurrenciePipe();
    expect(pipe).toBeTruthy();
  });
});
