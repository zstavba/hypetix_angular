import { SearchPaymentTermPipe } from './search-payment-term.pipe';

describe('SearchPaymentTermPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPaymentTermPipe();
    expect(pipe).toBeTruthy();
  });
});
