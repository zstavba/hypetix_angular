import { SearchInvoicesPipe } from './search-invoices.pipe';

describe('SearchInvoicesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInvoicesPipe();
    expect(pipe).toBeTruthy();
  });
});
