import { SearchChipperPipe } from './search-chipper.pipe';

describe('SearchChipperPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchChipperPipe();
    expect(pipe).toBeTruthy();
  });
});
