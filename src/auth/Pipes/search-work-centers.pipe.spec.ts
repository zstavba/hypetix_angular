import { SearchWorkCentersPipe } from './search-work-centers.pipe';

describe('SearchWorkCentersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchWorkCentersPipe();
    expect(pipe).toBeTruthy();
  });
});
