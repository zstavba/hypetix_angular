import { SearchSupplierOrderPipe } from './search-supplier-order.pipe';

describe('SearchSupplierOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSupplierOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
