import { SearchExchangeRatesPipe } from './search-exchange-rates.pipe';

describe('SearchExchangeRatesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchExchangeRatesPipe();
    expect(pipe).toBeTruthy();
  });
});
