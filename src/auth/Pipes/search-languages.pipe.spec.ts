import { SearchLanguagesPipe } from './search-languages.pipe';

describe('SearchLanguagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLanguagesPipe();
    expect(pipe).toBeTruthy();
  });
});
