import { SearchRateSheetsPipe } from './search-rate-sheets.pipe';

describe('SearchRateSheetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRateSheetsPipe();
    expect(pipe).toBeTruthy();
  });
});
