import { ArticleTypeSearchPipe } from './article-type-search.pipe';

describe('ArticleTypeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleTypeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
