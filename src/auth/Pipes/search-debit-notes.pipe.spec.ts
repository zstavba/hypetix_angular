import { SearchDebitNotesPipe } from './search-debit-notes.pipe';

describe('SearchDebitNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDebitNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
