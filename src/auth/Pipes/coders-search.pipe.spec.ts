import { CodersSearchPipe } from './coders-search.pipe';

describe('CodersSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CodersSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
