import { SearchGroupPipe } from './search-group.pipe';

describe('SearchGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
