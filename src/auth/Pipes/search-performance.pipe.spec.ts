import { SearchPerformancePipe } from './search-performance.pipe';

describe('SearchPerformancePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPerformancePipe();
    expect(pipe).toBeTruthy();
  });
});
