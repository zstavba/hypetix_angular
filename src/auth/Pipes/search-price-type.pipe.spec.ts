import { SearchPriceTypePipe } from './search-price-type.pipe';

describe('SearchPriceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPriceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
