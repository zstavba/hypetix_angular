import { SearchCharacteristicsPipe } from './search-characteristics.pipe';

describe('SearchCharacteristicsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCharacteristicsPipe();
    expect(pipe).toBeTruthy();
  });
});
