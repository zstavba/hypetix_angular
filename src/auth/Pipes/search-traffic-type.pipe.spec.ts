import { SearchTrafficTypePipe } from './search-traffic-type.pipe';

describe('SearchTrafficTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTrafficTypePipe();
    expect(pipe).toBeTruthy();
  });
});
