import { SearchTaxPipe } from './search-tax.pipe';

describe('SearchTaxPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTaxPipe();
    expect(pipe).toBeTruthy();
  });
});
